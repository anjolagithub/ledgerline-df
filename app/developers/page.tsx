import { ProductShell, SectionHeader } from '../../components/ProductShell'
export default function Developers() { return <ProductShell><SectionHeader eyebrow="LedgerLine / developers" title="Build on verified credit evidence." description="Integrate profile reads, receipt proofs, and registry state into your lending application."/><div className="grid gap-px bg-border md:grid-cols-2"><pre className="overflow-auto bg-[#060606] p-6 font-mono text-xs leading-6 text-slate-400">{`GET /v1/credit-profiles/:address\nGET /v1/attestations/:hash\nGET /v1/networks/status`}</pre><div className="bg-[#080808] p-6"><p className="eyebrow text-primary">Registry primitives</p><p className="mt-5 text-sm leading-6 text-slate-500">Every response maps back to a chain, transaction, and verification state.</p></div></div></ProductShell> }
