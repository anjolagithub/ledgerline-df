import Link from 'next/link'
import { ArrowUpRight, Info } from 'lucide-react'
import { ProductShell, SectionHeader } from '../../components/ProductShell'

export default function Financing() { return <ProductShell><SectionHeader eyebrow="LedgerLine / financing" title="Move from verified evidence to an informed decision." description="A lender workspace for reviewing proof-backed receivables. Creditcoin execution remains a clearly marked testnet handoff."/><section className="mb-6 border border-amber-400/30 bg-amber-400/[.04] p-5 font-mono text-[10px] text-amber-300"><div className="flex items-center gap-3"><Info size={14}/><span>DEMO UNDERWRITING POLICY · Sample values illustrate how evidence can inform eligibility; they are not a lender approval.</span></div></section><section className="grid gap-px bg-border md:grid-cols-2 lg:grid-cols-4">{[['Invoice','10,000 USDC','DEMO / TESTNET DATA'],['Credit profile','742','Read from CC3 when queried'],['Verified repayments','7','Use only when returned by registry'],['Eligible advance','8,000 USDC','DEMO UNDERWRITING POLICY']].map(([title,value,note])=><div key={title} className="bg-[#060606] p-5"><p className="eyebrow text-primary">{title}</p><p className="mt-5 font-mono text-xl text-slate-100">{value}</p><p className="mt-2 font-mono text-[9px] leading-4 text-slate-600">{note}</p></div>)}</section><section className="mt-8 border border-border bg-[#060606] p-5"><p className="eyebrow text-slate-500">Creditcoin transaction integration</p><h2 className="mt-2 text-sm font-semibold text-slate-200">Testnet handoff only</h2><p className="mt-3 max-w-xl text-xs leading-5 text-slate-500">No financing contract action is configured in this application. LedgerLine will not claim a Creditcoin transaction occurred. Connect the real financing contract before enabling execution.</p><span className="mt-5 inline-flex border border-border px-4 py-3 font-mono text-[10px] uppercase tracking-[.12em] text-slate-600">TESTNET DEMO · EXECUTION DISABLED</span></section><Link href="/dashboard/lender" className="mt-8 inline-flex items-center gap-2 border border-primary px-5 py-3 font-mono text-[10px] uppercase tracking-[.12em] text-primary">Open lender workspace <ArrowUpRight size={13}/></Link></ProductShell> }
