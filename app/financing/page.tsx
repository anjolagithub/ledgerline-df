import { ProductShell, SectionHeader } from '../../components/ProductShell'
import Link from 'next/link'
export default function Financing() { return <ProductShell><SectionHeader eyebrow="LedgerLine / financing" title="Move from verified invoice to action." description="A lender workspace for reviewing proof-backed receivables and initiating guarded testnet financing."/><section className="grid gap-px bg-border md:grid-cols-3">{[['Eligibility','Verified invoice evidence required.'],['Risk signal','Credit profile available on-chain.'],['Execution','Wallet confirmation required.']].map(([title,copy])=><div key={title} className="bg-[#060606] p-6"><p className="eyebrow text-primary">{title}</p><p className="mt-5 text-sm leading-6 text-slate-400">{copy}</p></div>)}</section><Link href="/dashboard/lender" className="mt-8 inline-flex border border-primary px-5 py-3 font-mono text-[10px] uppercase tracking-[.12em] text-primary">Open lender workspace →</Link></ProductShell> }
