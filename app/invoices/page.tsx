import Link from 'next/link'
import { ArrowUpRight, Info } from 'lucide-react'
import { ProductShell, SectionHeader } from '../../components/ProductShell'
import { demoInvoices, formatUSDC } from '../../lib/product-data'

export default function Invoices() { return <ProductShell><SectionHeader eyebrow="LedgerLine / invoices" title="Financing eligibility, grounded in evidence." description="Review demonstration receivables and route eligible evidence into the LedgerLine financing workflow."/><div className="mb-5 flex items-center gap-3 border border-amber-400/30 bg-amber-400/[.04] px-4 py-3 font-mono text-[10px] text-amber-300"><Info size={14}/><span>DEMO / TESTNET DATA · These records are sample entities, not LedgerLine customers or live invoice registry records.</span></div><div className="overflow-x-auto border border-border"><table className="min-w-[760px] w-full text-left font-mono text-xs"><thead className="border-b border-border text-[9px] uppercase tracking-[.12em] text-slate-600"><tr><th className="px-5 py-4">Invoice</th><th>Issuer</th><th>Counterparty</th><th>Value</th><th>Status</th><th></th></tr></thead><tbody>{demoInvoices.map(invoice => <tr key={invoice.id} className="border-b border-border text-slate-400"><td className="px-5 py-5 text-slate-200">{invoice.id}<span className="mt-1 block text-[9px] text-slate-600">{invoice.dataState}</span></td><td>{invoice.issuer}</td><td>{invoice.buyer}</td><td>{formatUSDC(invoice.amount)}</td><td className={invoice.status === 'Verified' ? 'text-primary' : 'text-amber-400'}>{invoice.status}</td><td><Link href="/financing" className="inline-flex items-center gap-1 text-primary">Review <ArrowUpRight size={12}/></Link></td></tr>)}</tbody></table></div></ProductShell> }
