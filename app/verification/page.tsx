import { ProductShell, SectionHeader } from '../../components/ProductShell'
export default function Verification() { return <ProductShell><SectionHeader eyebrow="LedgerLine / verification" title="Proofs you can inspect, not just trust." description="LedgerLine decodes receipts, verifies continuity, and anchors credit evidence across connected environments."/><div className="divide-y divide-border border-b border-border font-mono text-xs">{[['01','Receipt decoded','Ethereum Sepolia','Verified'],['02','Attestcoin schema proof','LedgerLine network','Verified'],['03','Credit score anchored','Creditcoin CC3','Verified']].map(([n,title,chain,status])=><div key={n} className="grid gap-3 px-5 py-6 md:grid-cols-[50px_1fr_180px_100px] md:items-center"><span className="text-primary">{n}</span><span className="text-slate-200">{title}</span><span className="text-slate-600">{chain}</span><span className="text-primary">{status}</span></div>)}</div></ProductShell> }
